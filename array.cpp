
#include <string>
#include <iostream>
#include <fstream>

bool readFile(std::ifstream fin, const std::string& filename, Matrix& mat)
{
	std::cout << "Reading file ..." << std::endl;
	return true;
}